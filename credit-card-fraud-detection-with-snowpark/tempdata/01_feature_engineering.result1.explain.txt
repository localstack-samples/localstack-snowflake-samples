---------DATAFRAME EXECUTION PLAN----------
Query List:
1.
SELECT "TX_FRAUD", "NB_TX_DAY", (RATIO_TO_REPORT("NB_TX_DAY") OVER (  ) * 100 :: INT) AS "PERCENTAGE" FROM ( SELECT "TX_FRAUD", count("TRANSACTION_ID") AS "NB_TX_DAY" FROM ( SELECT  *  FROM CUSTOMER_TRANSACTIONS_FRAUD) GROUP BY "TX_FRAUD")
Logical Execution Plan:
GlobalStats:
    partitionsTotal=1
    partitionsAssigned=1
    bytesAssigned=14747648
Operations:
1:0     ->Result  CUSTOMER_TRANSACTIONS_FRAUD.TX_FRAUD, COUNT(CUSTOMER_TRANSACTIONS_FRAUD.TRANSACTION_ID), ((COUNT(CUSTOMER_TRANSACTIONS_FRAUD.TRANSACTION_ID)) / (IFF(SUM(COUNT(CUSTOMER_TRANSACTIONS_FRAUD.TRANSACTION_ID)) OVER () = 0, null, SUM(COUNT(CUSTOMER_TRANSACTIONS_FRAUD.TRANSACTION_ID)) OVER ()))) * 100  
1:1          ->WindowFunction  SUM(COUNT(CUSTOMER_TRANSACTIONS_FRAUD.TRANSACTION_ID)) OVER ()  
1:2               ->Aggregate  aggExprs: [COUNT(CUSTOMER_TRANSACTIONS_FRAUD.TRANSACTION_ID)], groupKeys: [CUSTOMER_TRANSACTIONS_FRAUD.TX_FRAUD]  
1:3                    ->TableScan  TEST.PUBLIC.CUSTOMER_TRANSACTIONS_FRAUD  TRANSACTION_ID, TX_FRAUD  {partitionsTotal=1, partitionsAssigned=1, bytesAssigned=14747648}

--------------------------------------------
