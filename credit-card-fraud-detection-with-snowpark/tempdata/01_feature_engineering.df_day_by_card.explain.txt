---------DATAFRAME EXECUTION PLAN----------
Query List:
1.
SELECT  *  FROM ( SELECT to_date("TX_DATETIME") AS "TO_DATE(TX_DATETIME)", sum("TX_FRAUD") AS "NBR_FRAUD_TRX", count( DISTINCT "CUSTOMER_ID") AS "NBR_FRAUD_CARD" FROM ( SELECT  *  FROM CUSTOMER_TRANSACTIONS_FRAUD WHERE ("TX_FRAUD" = 1 :: INT)) GROUP BY to_date("TX_DATETIME")) ORDER BY "TO_DATE(TX_DATETIME)" ASC NULLS FIRST
Logical Execution Plan:
GlobalStats:
    partitionsTotal=1
    partitionsAssigned=1
    bytesAssigned=14747648
Operations:
1:0     ->Result  TO_DATE(CUSTOMER_TRANSACTIONS_FRAUD.TX_DATETIME), SUM(CUSTOMER_TRANSACTIONS_FRAUD.TX_FRAUD), COUNT(DISTINCT CUSTOMER_TRANSACTIONS_FRAUD.CUSTOMER_ID)  
1:1          ->Sort  TO_DATE(CUSTOMER_TRANSACTIONS_FRAUD.TX_DATETIME) ASC NULLS FIRST  
1:2               ->Aggregate  aggExprs: [SUM(CUSTOMER_TRANSACTIONS_FRAUD.TX_FRAUD), COUNT(DISTINCT CUSTOMER_TRANSACTIONS_FRAUD.CUSTOMER_ID)], groupKeys: [TO_DATE(CUSTOMER_TRANSACTIONS_FRAUD.TX_DATETIME)]  
1:3                    ->Filter  CUSTOMER_TRANSACTIONS_FRAUD.TX_FRAUD = 1  
1:4                         ->TableScan  TEST.PUBLIC.CUSTOMER_TRANSACTIONS_FRAUD  TX_DATETIME, CUSTOMER_ID, TX_FRAUD  {partitionsTotal=1, partitionsAssigned=1, bytesAssigned=14747648}

--------------------------------------------
